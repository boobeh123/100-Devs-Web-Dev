/**************************************************************
* Use JSX effectively with React
***************************************************************/
/*

* We can interpolate a variable's value into the children position.
    * Using curly braces we can embed JavaScript expressions or variable values.
        * The value is placed for React.createElement

* JSX can have self-closing tags and interpolation can be done with props.

* The extends helper is like object.assign
* Overriding a prop from a spread of props can be done by providing the prop after the spread.

*/
/**************************************************************
* Use JSX effectively with React
***************************************************************/