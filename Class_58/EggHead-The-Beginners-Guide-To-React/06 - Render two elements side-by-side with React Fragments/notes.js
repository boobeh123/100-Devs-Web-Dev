/**************************************************************
* Render two elements side-by-side with React Fragments
***************************************************************/
/*

* We want to render two elements with React.createElement.
    * How do we render two React Elements side by side?
        * We would need to use React.Fragment to accomplish this without JSX

* We want to render two elements with JSX.
    * How do we render two React Elements side by side?
        * React fragment allows us to put elements side by side without having a container element.

* React.Fragments help with creating tables in React.

* React.Fragment has a shorter syntax which is empty brackets <> </>

*/
/**************************************************************
* Render two elements side-by-side with React Fragments
***************************************************************/